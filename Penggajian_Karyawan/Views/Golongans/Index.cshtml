@model IEnumerable<Penggajian_Karyawan.Models.Golongan>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nama)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.JenisGolongan)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nama)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.JenisGolongan)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdGolongan"><i class="fa fa-marker fa-lg"></i></a> 
                <a asp-action="Details" asp-route-id="@item.IdGolongan"><i class="fas fa-list"></i></a> 
                <a asp-action="Delete" asp-route-id="@item.IdGolongan"><i class="fa fa-trash-alt fa-lg"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
